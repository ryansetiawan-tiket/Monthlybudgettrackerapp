# Desktop Transaction Entry - Visual Mockups

**Feature:** Desktop CTA Button & Unified Transaction Dialog  
**Design System:** Existing app design (dark mode, neutral palette)  
**Date:** November 8, 2025

---

## 📐 DESIGN SPECIFICATIONS

### Color Palette (Existing)
```
Primary Background:    #0A0A0A (neutral-950)
Card Background:       #171717 (neutral-900)
Border:                #262626 (neutral-800)
Text Primary:          #FAFAFA (neutral-50)
Text Secondary:        #A3A3A3 (neutral-400)
Accent Red:            #EF4444 (red-500)
Accent Green:          #10B981 (emerald-500)
Button Primary:        Default from design system
Button Hover:          Slightly lighter
```

### Typography (Existing)
```
Title (Daftar Transaksi):  text-base sm:text-lg
Button Text:               text-sm
Tab Labels:                text-sm font-medium
Form Labels:               text-sm
```

### Spacing (Existing)
```
Card Padding:   p-4 sm:p-6
Gap Between:    gap-2, gap-3, gap-4
Button Padding: px-3 py-2 (size="sm")
```

---

## 🎨 VISUAL MOCKUP 1: Desktop Header (Before)

### Current State (Without Button)

```
┌────────────────────────────────────────────────────────────┐
│  Daftar Transaksi Card                                     │
│  ──────────────────────────────────────────────────────── │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  Daftar Transaksi                              [📊] │ │ ← Header Row 1
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  [Pilih] [Badge]              [🔒] -Rp 4.168.170    │ │ ← Header Row 2
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  [Pengeluaran] [Pemasukan]                           │ │ ← Header Row 3
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  [🔍 Search ························] [🔄]         │ │ ← Header Row 4
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  (Transaction List Items)                                  │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

**Problem:** ❌ No clear way to add transactions!

---

## 🎨 VISUAL MOCKUP 2: Desktop Header (After - NEW!)

### New State (With Button)

```
┌────────────────────────────────────────────────────────────┐
│  Daftar Transaksi Card                                     │
│  ──────────────────────────────────────────────────────── │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  Daftar Transaksi                                    │ │
│  │                    [+ Tambah Transaksi] [📊]       │ │ ← NEW BUTTON!
│  │                     ↑                   ↑            │ │
│  │                     NEW!          Existing           │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  [Pilih] [Badge]              [🔒] -Rp 4.168.170    │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  [Pengeluaran] [Pemasukan]                           │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  [🔍 Search ························] [🔄]         │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  (Transaction List Items)                                  │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

**Solution:** ✅ Clear, discoverable CTA button!

---

## 🎨 VISUAL MOCKUP 3: Button States

### Normal State
```
┌─────────────────────────┐
│  + Tambah Transaksi    │  ← Default button
└─────────────────────────┘
   │                   │
   ↓                   ↓
Plus Icon       Text Label
(size-4)        (text-sm)
```

**Styling:**
```tsx
variant="default"
size="sm"
className="hidden md:flex items-center gap-1.5"
```

### Hover State
```
┌─────────────────────────┐
│  + Tambah Transaksi    │  ← Slightly lighter bg
└─────────────────────────┘
   Cursor: pointer
   Background: hover state from design system
```

### Focus State (Keyboard)
```
┌─────────────────────────┐
│  + Tambah Transaksi    │  ← Focus ring visible
└─────────────────────────┘
   Ring: focus-visible:ring-2
```

---

## 🎨 VISUAL MOCKUP 4: Button Layout Detail

### Header Row Layout (Flexbox)

```
┌────────────────────────────────────────────────────────────┐
│  <div class="flex items-center justify-between">          │
│                                                            │
│    ┌─────────────────┐                 ┌──────────────┐  │
│    │ Daftar Transaksi│                 │              │  │
│    └─────────────────┘                 │              │  │
│    (flex-start)                        │ (flex-end)   │  │
│                                        │              │  │
│                                  ┌─────┴──────┬───────┘  │
│                                  │            │          │
│                            ┌─────▼────┐  ┌────▼────┐    │
│                            │ + Tambah │  │   📊   │    │
│                            │ Transaksi│  │         │    │
│                            └──────────┘  └─────────┘    │
│                            (gap-2)                       │
│                            <div class="flex gap-2">      │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

**Code:**
```tsx
<div className="flex items-center justify-between">
  {/* Left: Title */}
  <span className="text-base sm:text-lg">Daftar Transaksi</span>
  
  {/* Right: Buttons */}
  <div className="flex items-center gap-2">
    {/* NEW: Add Button (desktop only) */}
    <Button
      variant="default"
      size="sm"
      onClick={onOpenAddTransaction}
      className="hidden md:flex items-center gap-1.5"
    >
      <Plus className="size-4" />
      Tambah Transaksi
    </Button>
    
    {/* Existing: Category Breakdown */}
    <DropdownMenu>{/* ... */}</DropdownMenu>
  </div>
</div>
```

---

## 🎨 VISUAL MOCKUP 5: Unified Transaction Dialog

### Dialog Structure (Desktop)

```
┌────────────────────────────────────────────────────────────┐
│  Tambah Transaksi                                      [X] │ ← Dialog Header
│  ──────────────────────────────────────────────────────── │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ ┌──────────────────┬──────────────────┐             │ │
│  │ │  Pengeluaran    │  Pemasukan       │             │ │ ← Segmented Control
│  │ └──────────────────┴──────────────────┘             │ │   (Tabs)
│  │      ↑ Active (red underline)                       │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │                                                      │ │
│  │  (Form Content - Changes based on selected tab)     │ │
│  │                                                      │ │
│  │  If Pengeluaran:                                    │ │
│  │    - AddExpenseForm (existing component)            │ │
│  │                                                      │ │
│  │  If Pemasukan:                                      │ │
│  │    - AdditionalIncomeForm (existing component)      │ │
│  │                                                      │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │                         [Batal]  [Simpan]           │ │ ← Form Buttons
│  └──────────────────────────────────────────────────────┘ │   (From forms)
│                                                            │
└────────────────────────────────────────────────────────────┘
```

**Dimensions:**
```
Width:     max-w-2xl (672px)
Max Height: 90vh (scrollable if taller)
Padding:   Standard dialog padding
Border:    Rounded corners
Shadow:    Standard dialog shadow
Backdrop:  Semi-transparent dark overlay
```

---

## 🎨 VISUAL MOCKUP 6: Segmented Control Detail

### Tabs Component (Segmented Control)

```
┌────────────────────────────────────────────────────────────┐
│  <Tabs value={selectedTab} onValueChange={setSelectedTab}> │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  <TabsList class="grid w-full grid-cols-2">          │ │
│  │                                                      │ │
│  │  ┌──────────────────┬──────────────────┐           │ │
│  │  │  Pengeluaran    │  Pemasukan       │           │ │
│  │  └──────────────────┴──────────────────┘           │ │
│  │         ↑                    ↑                     │ │
│  │   Active (selected)    Inactive                    │ │
│  │   - Red text                                       │ │
│  │   - Red underline                                  │ │
│  │   - Font: font-medium                              │ │
│  │                                                      │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

**Active Tab Styling:**
```
Background: Slightly lighter
Text: Red (#EF4444)
Underline: Red bar (2px)
Font Weight: medium
```

**Inactive Tab Styling:**
```
Background: Transparent
Text: Gray (#A3A3A3)
Underline: None
Font Weight: normal
```

---

## 🎨 VISUAL MOCKUP 7: Dialog Content (Pengeluaran Tab)

### When "Pengeluaran" Tab Selected

```
┌────────────────────────────────────────────────────────────┐
│  Tambah Transaksi                                      [X] │
│  ──────────────────────────────────────────────────────── │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ [Pengeluaran] [Pemasukan]                            │ │
│  │  ↑ Active                                            │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  ┌─ AddExpenseForm ─────────────────────────────┐   │ │
│  │  │                                               │   │ │
│  │  │  Nama Pengeluaran                             │   │ │
│  │  │  [                                         ]  │   │ │
│  │  │                                               │   │ │
│  │  │  Nominal                                      │   │ │
│  │  │  [                                         ]  │   │ │
│  │  │                                               │   │ │
│  │  │  Kategori                                     │   │ │
│  │  │  [ Pilih kategori ▼                        ]  │   │ │
│  │  │                                               │   │ │
│  │  │  Tanggal                                      │   │ │
│  │  │  [DD/MM/YYYY]                                │   │ │
│  │  │                                               │   │ │
│  │  │  Kantong                                      │   │ │
│  │  │  [ Daily Spending ▼                        ]  │   │ │
│  │  │                                               │   │ │
│  │  │                        [Batal]  [Simpan]     │   │ │
│  │  │                                               │   │ │
│  │  └───────────────────────────────────────────────┘   │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

**Note:** This reuses the existing `AddExpenseForm` component exactly as-is!

---

## 🎨 VISUAL MOCKUP 8: Dialog Content (Pemasukan Tab)

### When "Pemasukan" Tab Selected

```
┌────────────────────────────────────────────────────────────┐
│  Tambah Transaksi                                      [X] │
│  ──────────────────────────────────────────────────────── │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ [Pengeluaran] [Pemasukan]                            │ │
│  │                ↑ Active                              │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  ┌─ AdditionalIncomeForm ───────────────────────┐   │ │
│  │  │                                               │   │ │
│  │  │  Nama Pemasukan                               │   │ │
│  │  │  [                                         ]  │   │ │
│  │  │                                               │   │ │
│  │  │  Nominal                                      │   │ │
│  │  │  [                                         ]  │   │ │
│  │  │                                               │   │ │
│  │  │  Mata Uang                                    │   │ │
│  │  │  [ IDR ▼ ]                                   │   │ │
│  │  │                                               │   │ │
│  │  │  (Exchange rate fields if USD selected)       │   │ │
│  │  │                                               │   │ │
│  │  │  Tanggal                                      │   │ │
│  │  │  [DD/MM/YYYY]                                │   │ │
│  │  │                                               │   │ │
│  │  │  Kantong Tujuan                               │   │ │
│  │  │  [ Daily Spending ▼                        ]  │   │ │
│  │  │                                               │   │ │
│  │  │  Potongan                                     │   │ │
│  │  │  [                                         ]  │   │ │
│  │  │                                               │   │ │
│  │  │                        [Batal]  [Simpan]     │   │ │
│  │  │                                               │   │ │
│  │  └───────────────────────────────────────────────┘   │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

**Note:** This reuses the existing `AdditionalIncomeForm` component exactly as-is!

---

## 🎨 VISUAL MOCKUP 9: Responsive Behavior

### Desktop (≥768px) - Button Visible

```
┌────────────────────────────────────────────────────────────┐
│                                                            │
│  Daftar Transaksi    [+ Tambah Transaksi] [📊]          │ ← Button shows
│                       ↑ visible!                          │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

**CSS:** `hidden md:flex` → Shows on medium+ screens

---

### Mobile (<768px) - Button Hidden

```
┌──────────────────────────────┐
│                              │
│  Daftar Transaksi      [📊] │ ← No button
│                              │   (Has FAB instead)
│                              │
└──────────────────────────────┘
```

**CSS:** `hidden md:flex` → Hidden on small screens

**Why:** Mobile has FAB (Floating Action Button) at bottom-right

---

## 🎨 VISUAL MOCKUP 10: Interaction States

### State 1: Default (Dialog Closed)
```
Button visible in header
Dialog NOT rendered (or rendered but closed)
```

### State 2: Button Clicked
```
Button clicked
    ↓
setIsTransactionDialogOpen(true)
    ↓
Dialog opens with animation (fade + scale)
    ↓
Default tab: "Pengeluaran" selected
    ↓
Focus moves to first form field
```

### State 3: Tab Switching
```
User clicks "Pemasukan" tab
    ↓
setSelectedTab('income')
    ↓
Form content switches with smooth transition
    ↓
Income form now visible
```

### State 4: Form Submission (Success)
```
User fills form & clicks "Simpan"
    ↓
Form validates & submits
    ↓
onOpenChange(false) called
    ↓
Dialog closes with animation
    ↓
Toast: "Pengeluaran berhasil ditambahkan"
    ↓
Transaction appears in list
```

### State 5: Form Cancellation
```
User clicks "Batal" / "X" / Escape / Backdrop
    ↓
onOpenChange(false)
    ↓
Dialog closes
    ↓
Form data discarded
    ↓
Tab resets to "Pengeluaran" (for next open)
```

---

## 🎨 VISUAL MOCKUP 11: Animation & Transitions

### Dialog Open Animation
```
Initial:  opacity: 0, scale: 0.95
    ↓  (150ms ease-out)
Final:    opacity: 1, scale: 1
```

### Dialog Close Animation
```
Initial:  opacity: 1, scale: 1
    ↓  (150ms ease-in)
Final:    opacity: 0, scale: 0.95
```

### Tab Switch Animation
```
Old content:  opacity: 1 → opacity: 0 (100ms)
New content:  opacity: 0 → opacity: 1 (100ms)
Total:        200ms smooth transition
```

**Implementation:** Built into shadcn/ui Dialog and Tabs components

---

## 🎨 VISUAL MOCKUP 12: Accessibility Features

### Keyboard Navigation
```
Tab Key:        Navigate between focusable elements
Shift+Tab:      Navigate backwards
Enter:          Activate focused button
Escape:         Close dialog
Arrow Keys:     Switch tabs (in tab list)
```

### Focus Management
```
Dialog Opens:
  → First form field auto-focused
  → Focus trapped inside dialog
  → Tab cycles within dialog only

Dialog Closes:
  → Focus returns to "Tambah Transaksi" button
```

### Screen Reader
```
Button:     aria-label="Tambah Transaksi"
Dialog:     role="dialog", aria-modal="true"
Tabs:       role="tablist", role="tab", role="tabpanel"
Form Fields: Proper labels associated
```

---

## 🎨 COLOR SAMPLES

### Button Colors
```
Default State:
  Background:  hsl(var(--primary))
  Text:        hsl(var(--primary-foreground))
  Border:      None

Hover State:
  Background:  hsl(var(--primary) / 0.9)
  Text:        hsl(var(--primary-foreground))

Focus State:
  Background:  hsl(var(--primary))
  Ring:        2px focus ring
```

### Tab Colors
```
Active Tab:
  Text:        #EF4444 (red-500)
  Background:  Slightly lighter than card
  Indicator:   Red underline (2px)

Inactive Tab:
  Text:        #A3A3A3 (neutral-400)
  Background:  Transparent
  Indicator:   None
```

---

## 📐 EXACT MEASUREMENTS

### Button
```
Height:      32px (size="sm")
Padding X:   12px
Padding Y:   8px
Gap:         6px (between icon & text)
Icon Size:   16px (size-4)
Font Size:   14px (text-sm)
Border Radius: 6px (rounded-md)
```

### Dialog
```
Max Width:     672px (max-w-2xl)
Max Height:    90vh
Padding:       24px
Border Radius: 12px (rounded-lg)
Shadow:        Large dialog shadow
```

### Tabs
```
Tab List:      Full width, 2 columns (grid-cols-2)
Tab Height:    40px
Tab Padding:   8px 16px
Indicator:     2px height, red color
Border Radius: 6px (rounded-md)
```

---

## 🎯 DESIGN CHECKLIST

- [x] Button design specified
- [x] Button positioning defined
- [x] Desktop-only visibility explained
- [x] Dialog structure documented
- [x] Segmented control designed
- [x] Tab switching behavior defined
- [x] Form layouts shown
- [x] Responsive behavior specified
- [x] Animation details provided
- [x] Accessibility features outlined
- [x] Color palette maintained
- [x] Measurements provided
- [x] All states documented

---

**Visual Mockups Complete!** 🎨  
**Ready for Implementation!** 🚀

---

**Next:** Proceed to [IMPLEMENTATION_GUIDE.md](IMPLEMENTATION_GUIDE.md)
